function makeNegative(num) {
    // Code?
    if (num>0) {
      return -num;
    } 
      return num;
    
  }
  console.log(makeNegative(-15));

  function makeNegative(num) {
    return num > 0 ? -num : num;
  }
  